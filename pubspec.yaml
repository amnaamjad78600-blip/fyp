name: ai_fashion_app
description: A new Flutter project for AI Fashion App.

# This prevents accidental publishing of your app to pub.dev
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
   
   flutter:
    sdk: flutter

   http: ^1.2.0
   firebase_core: ^2.30.0
   firebase_auth: ^4.17.0
   cloud_firestore: ^4.15.0
   firebase_storage: ^11.6.0
   image_picker: ^1.0.4
   path_provider: ^2.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
